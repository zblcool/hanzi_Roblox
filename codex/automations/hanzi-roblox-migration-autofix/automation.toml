version = 1
id = "hanzi-roblox-migration-autofix"
name = "Hanzi Roblox Migration Autofix"
prompt = "Use the first cwd as the local Roblox migration workspace and the second cwd as the hanziHero source repository. Treat the active Roblox Studio project named `Rogue Survivor` as the target experience. Before any modification, list Roblox Studio instances and confirm the active Studio matches that project; if it does not, stop and report the mismatch instead of guessing. Treat GitHub repo `zblcool/gameDev` as the target issue tracker unless a future local note overrides it. This target may not be git-backed, so do not assume git status, commit, branch, or push is available or required. Use Roblox Studio MCP tools to inspect and edit the live game directly, and use the local workspace only for migration notes, checklist files, and the GitHub bot wrapper. Scan GitHub for newly opened or unprocessed issues in `zblcool/gameDev`. Only process issues, not pull requests. Use the bot-authenticated wrapper at `./scripts/gh-bot` for all GitHub issue operations. Prefer `./scripts/gh-bot issue list`, `issue view`, `issue edit`, `issue comment`, and `issue create` first, and only use `./scripts/gh-bot api` when the issue subcommands cannot do the job, always through the approved external bot path. If GitHub access is unavailable, stop and report that blocker. For each eligible issue, read the title, body, labels, and comments, decide whether it is clear and safe, add an in progress label if needed, optionally leave the exact short comment `Started working on this.`, make the smallest safe fix in the active Roblox Studio project, run relevant validation inside Studio such as tree inspection, script reads, play tests, console checks, or screen captures, and only then leave a short public issue comment in the form `Fixed. Summary: one sentence. Validation: one sentence.` mentioning `@zblcool`. If an issue cannot be safely fixed, do not mark it fixed; leave a short public comment in the form `I investigated this but could not complete a safe fix yet. Blocker: one sentence. Needed: one sentence.` mentioning `@zblcool`. If there is no eligible open issue in the target repository, switch to migration mode. In migration mode, inspect the hanziHero source repository, including its root README and implemented UI, screens, systems, progression, and polish, and compare them against the active Roblox Studio game. Maintain a root file named `ROBLOX_MIGRATION_CHECKLIST.md` in the first cwd. Create it if it does not exist. Keep it as a long-lived migration checklist grouped into clear sections such as front-end and launcher, gameplay loop, combat and enemies, progression and content, HUD and UX, audio and polish, mobile and platform, and technical debt. Mark items as done, in progress, pending, or blocked, and keep short notes on what exists in hanziHero and what is still missing in Roblox. After refreshing the checklist, pick at most one small or medium, clearly scoped, low-risk unchecked migration item and implement it in the active Roblox Studio project. Preserve existing Roblox structure and naming, avoid unrelated systems, validate in Studio using play mode and/or script inspection and console output when appropriate, then update `ROBLOX_MIGRATION_CHECKLIST.md` to reflect what was completed, what remains, and what is blocked. If a migration item is too large or risky, split it into smaller checklist items instead of attempting a large refactor. Do not assume cloud publish or git versioning is available; if publishing to Roblox or saving to a remote version is required and no supported tool exists, stop after the local Studio implementation and report that manual publish remains. Never expose secrets, tokens, internal-only details, security-sensitive information, or private reasoning. Keep any public GitHub comments brief and professional, and mention `@zblcool` in those comments so notifications fire. Prefer steady migration progress over large rewrites. Always open an inbox item summarizing which issue was handled or which checklist item was migrated, what changed, how it was validated, and what the next recommended migration item is."
status = "PAUSED"
rrule = "FREQ=HOURLY;INTERVAL=1"
execution_environment = "local"
model = "gpt-5.4"
reasoning_effort = "xhigh"
cwds = ["/Users/ash/Documents/Github/Roblox_project", "/Users/ash/Documents/Github/hanziHero"]
created_at = 1774283760000
updated_at = 1774345855847
